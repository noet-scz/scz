# SCZ — баги и задачи (клиент-узел + зона)

Открытые (из тестов пользователя):

- [ ] **B1. Дизайн зоны не совпадает с расширением.** Главное требование: перенести
  внешний вид расширения (web/ + dist/app), а не выдумывать свой. Сейчас вёрстка
  расходится. Порт: шапка/навигация, чип личности, компоненты, отступы, типографика.
- [ ] **B2. Аватар не грузится.** Картинка профиля (imgur) показывается битой, фолбэк на
  идентикон не срабатывает надёжно. Нужно: идентикон по умолчанию, подмена на картинку
  только если она реально загрузилась.
- [ ] **B3. Свой сайт не открывается.** Под занятым именем (nyx.me) страница, опубликованная
  через расширение, не находится («ничего не добавлено»), хотя в расширении работает.
  Резолв владельца/страницы и форматы контента ({html}, IPFS cid, сырой html) обработать
  лениво и совместимо с расширением.
- [ ] **B4. RU/EN из интерфейса убрать в настройки.** Не пихать переключатель языка в
  сайдбар, перенести в настройки.
- [ ] **B5. Настройки приложения.** Отдельный экран настроек: занятая память/хранилище,
  выбор языка, импорт ключа, несколько аккаунтов (добавление и переключение), видно в
  какой аккаунт вошёл, тумблер уведомлений, прочее.

Решённые:
- [x] Перенос на нативный клиент Tauri (узел + зона в браузере).
- [x] Тег отдельно от ника.
- [x] Мессенджер: каналы слева, чат справа.
- [x] Сайт открывается на весь экран.
- [x] Убрана дублирующая главная.
