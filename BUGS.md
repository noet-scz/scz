# noet — баги и задачи (клиент-узел + зона)

Открытые:

- [ ] **Иконки.** Завести свой набор иконок (не lucide). Иконочные кнопки заменяют текст там,
  где смысл очевиден (назад, копировать, сохранить, отправить, переключить, открыть). Сделано
  частично, добить остальные поверхности.

Решённые:
- [x] Перенос на нативный клиент Tauri (узел + зона в браузере).
- [x] B1 дизайн зоны перенесён с расширения: верхняя шапка, палитра, радиальный фон,
  градиентные карточки, профиль с idhead (аватар + имя + @тег).
- [x] B2 аватар: идентикон по умолчанию, картинка подменяется только если загрузилась.
- [x] B3 сайт: ленивый резолв страницы (владелец/свежая), форматы {html}/IPFS cid/сырой html,
  на весь экран, единая шапка как везде.
- [x] B4 RU/EN убран из интерфейса в настройки.
- [x] B5 настройки В ПРИЛОЖЕНИИ: нативное окно = панель (аккаунты создать/добавить/переключить,
  виден активный, хранилище, язык, уведомления, обновление). В зоне настройки тоже есть.
- [x] Поиск как в расширении (главная зоны = поиск по именам/профилям/страницам).
- [x] Иконки: набор заведён, заменены назад/копировать/сохранить/отправить/переключить/открыть.
- [x] Тег отдельно от ника; мессенджер каналы|чат; убрана дублирующая главная.
