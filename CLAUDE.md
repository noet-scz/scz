# noet — правила проекта (read me before touching anything)

Проект: зона **noet** (TLD `.nt`), цензуроустойчивое сообщество. Код PoC в `poc/zone-portal/`.
Это файл-договор: нарушать пункты нельзя, даже если «и так сойдёт».

## Железные правила (нарушение = брак)

1. **Secure context / крипта.** Зона работает по **http** на придуманных доменах (`id.nt`,
   `search.nt`, …). На таком origin **нет `window.crypto.subtle`** (WebCrypto доступен только
   на https или localhost/127.0.0.1). Поэтому:
   - НИКОГДА не использовать `crypto.subtle.*` в коде зоны.
   - Хеши (sha256 для id события Nostr) — только чистый JS (`web/sha256` / noble/hashes).
   - `crypto.getRandomValues` можно (работает и на insecure).
   - noble schnorr (`vendor/noble-secp256k1.js`) подписи делает без subtle — ок.

2. **Проверяй в настоящем браузере, потом говори «готово».** Не верить curl'у про UX.
   Гонять реальный сценарий (регистрация, постинг) через headless Chrome по CDP
   (`google-chrome-stable --remote-debugging-port`, навигация, скриншот). Если флоу не
   прокликан в браузере — он не сделан. Проверять и insecure-origin (через прокси), а не
   только 127.0.0.1.

3. **Никаких сырых технических ошибок в UI.** Любой `await` оборачивать; пользователю
   показывать понятный локализованный текст. `Cannot read properties of undefined` на
   экране = брак. Логировать детали в консоль, на экран — человеческое.

4. **i18n RU/EN на всех видимых поверхностях.** Тексты и ошибки через словарь
   (`web/i18n.js`, `window.t(key)`), а не хардкодом. Дефолт RU, переключатель есть.

5. **UX-разделение.** Регистрация/вход/профиль — на **отдельной странице `id.nt`**, не в
   строке поиска и не формой в виджете. На поиске и в реле — только чип «ник+аватар» или
   «Гость», по клику переход на `id.nt`. Виджет — навигация и показ личности, без форм входа.

6. **Детерминированные потоки.** Один ясный путь, без «создай-или-импортируй-или-войди» в
   одной куче. Создал ключ → он сразу активен → дальше только хэндл+инвайт. Импорт — явная
   отдельная ветка для вернувшихся.

7. **Длинное тире `—` в контенте не использовать.** Запятая/двоеточие/точка.

8. **Не использовать ник пользователя (`nyx`) как пример/плейсхолдер.** Нейтральные примеры.

9. **Думать наперёд:** secure-context, кросс-ориджин, пустые состояния, состояния ошибки,
   мобайл, что увидит человек, который видит это впервые.

11. **НЕ трогать живые данные при тестах.** `members.json`/`relay-events.json`/`registry.json`
    это рабочее состояние пользователя. Никогда не обнулять их во время отладки. Тестовые
    личности создавать в ОДНОРАЗОВОМ профиле браузера (`--user-data-dir=/tmp/...`) и чистить
    только свои записи по pubkey. Один индикатор личности на всех страницах (чип виджета,
    фикс. правый верх) — не плодить чипы по страницам, они разъезжаются.

10. **Identity без iframe (popup-auth).** Больше нет iframe-моста (он ломался на cross-site).
    Архитектура: `widget.js` хранит токен и nsec в localStorage ТЕКУЩЕГО origin, вызывает
    `/api/me` напрямую. Логин = `window.open('http://id.nt/?popup=1')`. `id.nt` логинит и
    отправляет `{ev:'auth', token, nsec}` через `window.opener.postMessage`. Виджет принимает,
    сохраняет локально, закрывает popup. Подпись Nostr — локально через schnorr (dynamic import).
    Следствие: каждый origin хранит токен+nsec независимо. При первом входе popup обязателен.

## Архитектура (ТЕКУЩАЯ: расширение-клиент + реестр-сервер)

- **Клиент = браузерное расширение** (`ext/`, Chrome/Vivaldi основной; Firefox только статика).
  Замороженное, конфиг с GitHub Pages (`dist/config.json`, `dist/names.json`). `bg.js` перехватывает
  ТОЛЬКО зарегистрированные `*.nt/*.me` (declarativeNetRequest + webNavigation) → `view.html`;
  реальные домены (t.me) пропускает. App-хосты (`noet.nt`/`id.nt`/`relay.nt`) → top-level навигация
  на http-страницы реестра.
- **Рендер контента:** публичные шлюзы — тупик (dweb.link отдаёт байты но не выполняет JS; w3s.link
  выполняет JS но не находит наш DHT-контент). Поэтому `view.js` ФЕТЧИТ байты страницы
  (dweb.link/ipfs.io) и рисует в manifest-SANDBOX-странице `render.html` (CSP разрешает JS).
  `window.nostr`/`window.noet` проброшены из sandbox в view.js (подпись + публичные wss-реле).
- **Личность = Nostr-ключ В РАСШИРЕНИИ** (window.nostr NIP-07: `nostr-provider.js` MAIN + `bridge.js`
  ISOLATED + `popup.html`). `account.js`/`widget.js` предпочитают window.nostr, fallback на
  localStorage origin реестра (старый вход цел). Popup-моста БОЛЬШЕ НЕТ (правило 10 устарело).
- **window.noet** (`noet-sdk.js`): me/publish/query поверх публичных wss-реле. Данные приложений
  (статы, доски) = подписанные Nostr-события. Примеры `sites/clicker`, `sites/dashboard`.
- **Реестр = `server.mjs`** на staging 144.31.25.136 (Docker, владелец nyx). Хранит ТОЛЬКО имена +
  auth (`auth.mjs`) + Nostr-реле (`ws.mjs`) + seed-пин + пуш зеркала names.json на github. Имя =
  `[sub.]handle.me` из сессии (`meNameFor`). `/api/publish` (text/html), `/api/publish-dir` (папка→IPFS).
  `/api/resolve` отдаёт `raw` (редактор грузит сохранённое). `handle.me` авто при регистрации.
- `server.mjs` двухролевой (REGISTRY_URL), но демон-клиент РЕТАЙРЕН. `noet.nt` поиск, `id.nt` личность/
  редактор, `relay.nt` реле, `handle.me`/`sub.handle.me` страницы участника, контент в IPFS.

## Команды

- Деплой на staging: `tar czf - web server.mjs | ssh root@144.31.25.136 "tar xzf - -C /root/scz-portal"`
  (SSH через SSH_ASKPASS-трюк). После server.mjs — `docker restart scz-portal`. `dist/*` → git push (Pages).
- Сборка расширения: `node build-ext.mjs` → `dist/noet-chrome.zip`, `noet-firefox.xpi`. Правка КОДА `ext/`
  → пользователь ПЕРЕустанавливает; правка `dist/config.json`/`names.json` → нет (тянется в рантайме).
- Инвайт (основатель): `curl -X POST -H "x-admin-token: <ADMIN>" http://144.31.25.136:8090/api/invite`.
- Проверка: headless Chrome НЕ грузит расширения → плумбинг тестит пользователь; крипту/серверное в node.

## Честные времянки (назвать, не прятать)

Привратник = основатель (выдаёт инвайты). Реестр имён = сервер-судья. Ключ в localStorage,
бэкап обязателен, паскей-восстановление — следующий шаг. Всё это §2.1/§7.2 концепции.
